packages:
  - "packages/*"
  - "apps/*"

# Both ship native/prebuilt binaries and need their install scripts to run.
# Declared here so a fresh clone builds without an interactive approval prompt.
allowBuilds:
  better-sqlite3: true
  esbuild: true
